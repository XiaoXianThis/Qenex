# Qenex 重写 · 开发指导

本文档根据架构讨论定稿，用于**一步一步开发与验收**。  
原则：**尽量使用现成框架/库，避免自研协议**；不兼容旧 Qenex 契约；版本管理 / 撤回本期不做。

---

## 1. 目标与非目标

### 目标

- 统一一套 Agent 聊天 UI，后端可接任意 ACP Agent（MVP 只保证 OpenCode）。
- 四端复用同一 UI：Web（纯壳）→ Desktop → VS Code → JetBrains。
- 前端走 **Vercel AI SDK 官方 UIMessage 协议**；ACP 细节交给现成 provider。

### 非目标（本期不做）

- 兼容旧 Qenex / AG-UI / Rust Bridge 契约
- 自研 ACP↔自研事件层、自研 SSE 方言
- Git checkpoint / rewind / 版本管理
- Marketplace、多 Agent 安装器（先假定本机已装 OpenCode）
- 公网 SaaS（Web 只做本地 UI 壳，Bridge 跑在本机）

---

## 2. 技术定案（已锁定）

| 项 | 定案 |
|----|------|
| 语言 / 运行时 | TypeScript + **Bun** |
| AI SDK | **`ai@^7`** + `@ai-sdk/react@^4` |
| ACP → AI SDK | **`@mcpc-tech/acp-ai-provider`** |
| 聊天 UI | React + Vite + Tailwind + **Assistant-UI**（`@assistant-ui/react-ai-sdk@^1.4`） |
| 前端协议 | AI SDK **UIMessage** stream（`createUIMessageStreamResponse` / `toUIMessageStream`） |
| MVP Agent | **OpenCode**：`command: "opencode"`, `args: ["acp"]` |
| Web | 纯 UI 壳（Vite proxy → 本地 Bridge） |
| 审批 UX | 参考 **Codex Desktop**：Ask / Auto + 线程内 Approve/Reject 卡片 |
| 旧契约 | 不兼容，整仓重写 |

### 推荐依赖骨架

```json
{
  "dependencies": {
    "ai": "^7",
    "@ai-sdk/react": "^4",
    "@assistant-ui/react": "latest",
    "@assistant-ui/react-ai-sdk": "^1.4",
    "@mcpc-tech/acp-ai-provider": "latest"
  }
}
```

> **注意：** provider README 可能仍写「对齐 AI SDK v6」，但 AI SDK v7 官方社区 Provider 页已收录该包。  
> **Phase 0 Spike 必须先验证 peer / 类型是否与 `ai@7` 合得来**；不合再 pin 明确支持 v7 的版本。

### 架构一句话

```
四端薄壳 → packages/ui (Assistant-UI + useChatRuntime)
         → UIMessage HTTP/SSE
         → apps/bridge (Bun + streamText)
         → @mcpc-tech/acp-ai-provider
         → opencode acp (stdio ACP)
```

---

## 3. 建议仓库结构

```
apps/
  bridge/          # Bun：/api/chat + session +（可选）审批回写
  web/             # Vite 纯 UI 壳
  desktop/         # Tauri（后置）
  vscode/          # 扩展 + webview（后置）
  jetbrains/       # 插件 + JCEF（后置）
packages/
  ui/              # 共享聊天 UI + 审批卡片
  core/            # session / agent / bridge client（与 Host 无关）
  platform/        # Host 抽象（web 先实现）
```

MVP 只强制：`apps/bridge` + `apps/web` + `packages/ui` + `packages/core`（`platform` 可先内联在 web）。

### Host 最小接口（四端统一）

```ts
type Host = {
  kind: "web" | "desktop" | "vscode" | "jetbrains";
  getBridgeBaseUrl(): string | Promise<string>;
  fetch: typeof fetch;
  storage: { get(key: string): string | null; set(key: string, value: string): void; remove(key: string): void };
  pickWorkspace?(): Promise<string | null>;
};
```

UI **不得**直接 import Tauri / VS Code / JetBrains API。

---

## 4. Bridge 写什么 / 不写什么

### 写（薄）

1. `POST /api/sessions` — 创建 ACP session（cwd + OpenCode），`initSession()` 预热  
2. `DELETE /api/sessions/:id` — `provider.cleanup()`  
3. `POST /api/chat` — 标准 AI SDK messages → `streamText` → UIMessage stream  
4. Session 内存 `Map`（持久化可后置）  
5. 审批：优先 AI SDK / provider 官方能力；不行再加**最小** REST 胶水  
6. OpenCode 探测（未安装给出明确错误）

### 不写

- 自研协议翻译层、AG-UI、复杂 layout 引擎  
- Diff rewind / checkpoint  
- 多 Agent registry / 安装器  

### Bridge 核心示意

```ts
const provider = createACPProvider({
  command: "opencode",
  args: ["acp"],
  session: { cwd, mcpServers: [] },
  persistSession: true,
});
await provider.initSession(); // 预热，降低 TTFT

const result = streamText({
  model: provider.languageModel(),
  messages: await convertToModelMessages(messages),
  tools: provider.tools,
  includeRawChunks: true, // plan / diff / terminal
});

return createUIMessageStreamResponse({
  stream: toUIMessageStream({
    stream: result.stream,
    messageMetadata: ({ part }) => {
      // 可选：raw → plan / diffs / terminals
    },
  }),
});
```

---

## 5. 审批 UX（Codex 逻辑）

| 概念 | MVP 行为 |
|------|----------|
| **Ask** | 敏感操作出线程内卡片，Approve / Reject |
| **Auto** | 工作区内尽量自动；需 escalation 再出卡 |
| **展示** | 工具名、路径/命令摘要（来自 permission / tool 载荷） |
| **不做** | 撤回、跨 session 复杂白名单（「本 session Always allow」可选） |

### 实现优先级

1. **优先**：AI SDK v7 `toolApproval` + Assistant-UI `respondToApproval`  
2. **其次**：provider 把 permission 映射成 tool / approval part，用官方 Tool UI  
3. **兜底**：Bridge 持有 pending permission Promise + 小 REST；**禁止**再发明第二套前端协议  

> OpenCode 工具在 Agent 进程内执行，权限来自 ACP `request_permission`，不一定等于带 `execute` 的普通 AI SDK tool。  
> **Phase 0 / Phase 3 必须 spike 清这条缝。**

---

## 6. 分阶段开发与验收

每阶段：**先做 → 自测清单全勾 → 再进下一阶段**。  
未通过不要堆下一层功能。

---

### Phase 0 · Spike（必做，0.5～1 天）

**目的：** 验证 `ai@7` + `@mcpc-tech/acp-ai-provider` + OpenCode，再铺 monorepo。

**步骤**

1. 本机确认：`opencode` 在 PATH，能跑 `opencode acp`（或文档等价入口）。  
2. 新建临时 Bun/TS 脚本（可丢在 `scratch/`，不必完美）。  
3. `createACPProvider` + `persistSession: true` + `initSession()`。  
4. `streamText` / `generateText` 跑一轮「你好」。  
5. 再跑一轮多轮，确认 session 记得上下文。  
6. 故意触发需权限的操作，观察流里是否出现 approval / tool / raw。  
7. 打开 `ACP_AI_PROVIDER_DEBUG=1` 看 agent 日志（排障用）。

**验收标准**

- [x] `ai@^7` 安装无 peer 致命冲突（或已记录可用的 pin 版本）  
- [x] 单轮流式输出可见文本  
- [x] 同 provider 第二轮能延续上下文  
- [x] 已记录：权限请求在流里的形态（approval / tool / 仅 raw / 需自建胶水）  
- [x] `provider.cleanup()` 后进程退出，无僵尸  

**实现位置：** `scratch/phase0/`（`bun run spike` / `bun test ./phase0.test.ts`）。详细结论见 `scratch/phase0/SPIKE.md` 与文末「Spike 结论」。

**失败处理**

- peer 不合：查 provider changelog / 换明确支持 v7 的版本；仍不行再评估短时锁 v6（需同步降 Assistant-UI，视为例外）。  
- OpenCode 起不来：先修本机 Agent，不进入 Phase 1。

---

### Phase 1 · Bridge 最小 HTTP

**目的：** 可被前端调用的本地 Bridge。

**步骤**

1. 建 Bun workspace：`apps/bridge`。  
2. 实现内存 Session Map。  
3. API：  
   - `POST /api/sessions` `{ cwd }` → `{ sessionId, modes?, models? }`  
   - `DELETE /api/sessions/:id`  
   - `POST /api/chat` 带 `sessionId` + AI SDK `messages` → UIMessage stream  
4. 监听 `127.0.0.1`（不要默认 `0.0.0.0`）。  
5. OpenCode 未找到时返回明确 JSON 错误。

**验收标准**

- [x] `curl` 创建 session 成功  
- [x] `curl`（或简单脚本）打 `/api/chat` 能收到流式 UIMessage  
- [x] 错误 sessionId → 4xx  
- [x] DELETE 后该 session 不可再用  
- [x] 仅本机可访问  

**实现位置：** `apps/bridge/`（`bun test` / `bun run test:acceptance`）。记录见 `apps/bridge/PHASE1.md`。

**建议契约（可微调，改了要更新本文档）**

```http
POST /api/sessions
Content-Type: application/json

{ "cwd": "/path/to/project" }

→ 200 { "sessionId": "...", "agent": "opencode" }
```

```http
POST /api/chat
Content-Type: application/json

{
  "sessionId": "...",
  "messages": [ /* UIMessage[] */ ]
}

→ 200 UIMessage stream（AI SDK 标准）
```

可选请求字段（Phase 3 再用）：`approvalMode: "ask" | "auto"`。

---

### Phase 2 · Web 壳 + Assistant-UI

**目的：** 浏览器里能聊，且走官方 Runtime。

**状态：通过**（2026-07-31）— 详见 `apps/web/PHASE2.md`

**步骤**

1. `apps/web`：Vite + React + Tailwind。  
2. proxy：`/api` → Bridge。  
3. `packages/ui`：`useChat` + `useAISDKRuntime` + `AssistantChatTransport({ api: "/api/chat" })`（不用 `useChatRuntime`：其 remote thread-list 嵌套会留下不可编辑 composer）。输入框与消息列表用本地/AI SDK 状态，绕开 Assistant-UI composer `setText` 与 `ThreadPrimitive.Messages` 在本接线中的渲染问题。  
4. 启动流：选/填 cwd → `POST /api/sessions` → 之后 chat 带上 `sessionId`（header 或 body，与 transport 约定一致）。  
5. 极简偏好：`localStorage` 存 last cwd / theme 即可。

**验收标准**

- [x] 打开 Web，选工作区，能发消息并看到流式回复  
- [x] 工具调用若有，Thread 中可见（至少不崩）  
- [x] 停止/取消生成可用（若 AI SDK/Assistant-UI 默认支持）  
- [x] 刷新后允许丢历史（本期可不持久化）  
- [x] UI 无直接依赖「非 Host」的桌面/IDE API  

---

### Phase 3 · Ask / Auto 审批

**目的：** Codex 风格审批，尽量用官方 approval 流。

**步骤**

1. 根据 Phase 0 记录选路径：官方 `toolApproval` / Tool UI / 最小 REST。  
2. Composer 旁：`Ask` | `Auto` 两档。  
3. 待审批卡片：摘要 + Approve / Reject（Deny 可带 reason）。  
4. Auto：能自动的不要弹；需升级时再弹。  
5. （可选）「本 session Always allow」。

**验收标准**

- [ ] Ask 下：会改文件/跑命令的操作出现卡片，Reject 后不执行（或 Agent 收到拒绝）  
- [ ] Approve 后对话继续，无卡死  
- [ ] Auto 下：工作区内常规操作不无故狂弹（允许偶发 escalation）  
- [ ] 切换 Ask/Auto 对后续请求生效  
- [ ] **没有**第二套自定义前端事件协议文档需求  

---

### Phase 4 · 体验增强（仍用库能力）

**目的：** 可读性，不做撤回。

**步骤**

1. `includeRawChunks` + `messageMetadata`：只读展示 plan / diff / terminal。  
2. 错误态文案：未安装 OpenCode、auth、spawn 失败。  
3. Session 列表（多 Tab 可选）：创建 / 切换 / 删除。  
4. （可选）极简 history adapter（`withFormat`）；不做 rewind。

**验收标准**

- [ ] 有 diff/plan 时 UI 能展示或优雅忽略，不崩  
- [ ] 常见错误用户能看懂下一步（去装 OpenCode 等）  
- [ ] 多 session 互不串话（若已做）  

---

### Phase 5 · Desktop 或 VS Code（二选一先做）

**目的：** 证明「薄壳 + 同一 UI」模式。

**共同步骤**

1. 壳启动 Bridge（随机端口）→ 把 `baseUrl` 注入 webview。  
2. 实现同一 `Host` 接口。  
3. 嵌入同一 `@…/ui` 构建产物。

**Desktop（Tauri）验收**

- [ ] 应用启动自动起 Bridge，聊天可用  
- [ ] 选目录走系统对话框  
- [ ] 退出后无残留 Bridge 僵尸（或可接受的明确清理逻辑）  

**VS Code 验收**

- [ ] 命令打开面板，webview 能聊  
- [ ] cwd 默认当前工作区  
- [ ] Extension 负责 spawn Bridge；webview 不直接起进程  

---

### Phase 6 · JetBrains + 打磨

**步骤**

1. JCEF + Kotlin 起 Bridge，注入 baseUrl。  
2. 实现 `Host`。  
3. 打包：Bridge 二进制 / Bun 分发策略（按平台）。  

**验收标准**

- [ ] IDEA 打开工具窗可聊 OpenCode  
- [ ] 主题明暗不致命破坏布局  
- [ ] 与 Web 功能基线一致（Ask/Auto、流式、错误态）  

---

## 7. 开发顺序总览

```
Phase 0  Spike OpenCode + ai@7 + acp-ai-provider
    ↓
Phase 1  Bridge /api/sessions + /api/chat
    ↓
Phase 2  Web + Assistant-UI Thread
    ↓
Phase 3  Ask/Auto 审批
    ↓
Phase 4  plan/diff 只读 + 错误态 + 可选多 session
    ↓
Phase 5  Desktop 或 VS Code
    ↓
Phase 6  JetBrains + 分发打磨
```

**不要**在 Phase 0 未通过时搭完整四端。  
**不要**为了「完美抽象」先写大量 platform 代码；Web 跑通后再抽 Host。

---

## 8. 质量与防回归约定

1. **能用库就用库**：AI SDK、Assistant-UI、acp-ai-provider、ACP 官方 SDK。胶水保持薄。  
2. **禁止**再引入平行协议（自研 AG-UI 方言、双 SSE 语义等），除非 Spike 证明官方路径不可用并书面记录原因。  
3. **Bridge 只绑 127.0.0.1**；后续若加 token，再开远程。  
4. **依赖 major 钉死**：`ai` / `@ai-sdk/react` / `@assistant-ui/react-ai-sdk` / `@mcpc-tech/acp-ai-provider` 一组升级、一组测。  
5. **OpenCode 为唯一 MVP 保证**；加 Claude/Codex 等是后续里程碑，不挡 MVP 验收。  
6. 每阶段在 PR/笔记里贴验收清单勾选结果。

---

## 9. 常见风险速查

| 风险 | 应对 |
|------|------|
| acp-ai-provider 与 ai@7 peer 不合 | Phase 0 尽早暴露；pin 兼容版或查官方 v7 文档 |
| 审批进不了 UIMessage | 最小 Bridge 胶水 + 同一套卡片；不新开协议 |
| 有状态 ACP vs 无状态 `/api/chat` | Session Map + `persistSession`；创建时 `initSession` |
| TTFT 慢 | 创建 session 时预热，勿每次 chat 冷启动 |
| Token usage 恒为 0 | UI 不依赖 usage 面板 |
| GUI 下 PATH 找不到 opencode | Desktop/IDE 继承/补全 login shell PATH |
| Windows `.cmd` shim | 后接其他 Agent 时再测；MVP 可先 macOS/Linux |

---

## 10. 开工检查清单（第一天）

- [x] 本机已装 Bun、OpenCode，终端可调用  
- [x] 读完本文 §2 定案与 §6 Phase 0  
- [x] 完成 Phase 0 并写下权限流结论（三选一或组合）  
- [x] 再创建 workspace / `apps/bridge`  
- [x] 仓库策略已定：本仓清空重写（当前工作区几乎空）或新仓  

---

## 11. 文档维护

- 若 API 路径、审批落点、依赖 major 有变更：**先改本文档，再改代码约定**。  
- Phase 0 的权限流结论建议追加一小节「Spike 结论」到文末，方便后人。

### Spike 结论（2026-07-31 · 已通过）

- 日期：2026-07-31  
- `ai` / `acp-ai-provider` 实际版本：`ai@7.0.44` / `@mcpc-tech/acp-ai-provider@0.3.4`（provider 声明 peer `ai@^6`，实测与 v7 可跑）  
- 多轮 session：通过（`persistSession: true`）  
- 权限在流中的形态：  
  - UIMessage / `fullStream`：**无** `approval-requested`  
  - 工作区内写文件：通常不调 ACP `requestPermission`；有 `tool-call` / `tool-result`  
  - 工作区外写文件：ACP `requestPermission`，options = `once` / `always` / `reject`  
  - provider 默认：未挂 handler 时自动选 `options[0]`（Allow once）；**无公开 permission API**  
- 审批拟采用路径：**最小 Bridge 胶水**（挂 ACP permission handler → 前端卡片 → 回写 optionId）。单靠 AI SDK `toolApproval` 不够。  
- 备注：复跑 `cd scratch/phase0 && bun run spike && bun test ./phase0.test.ts`；详见 `scratch/phase0/SPIKE.md`。  

---

*定案来源：架构讨论（AI SDK v7、@mcpc-tech/acp-ai-provider、OpenCode MVP、Web 纯壳、Codex 式审批、不兼容旧 Qenex、不做版本撤回）。*

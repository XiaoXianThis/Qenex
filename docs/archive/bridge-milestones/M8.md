# M8 完成记录 — 退役与发布（v0.3.0）

**状态：通过**（2026-08-03）

## 范围（对照 [`重构指导.md`](../../../重构指导.md) §M8 / §7）

1. 删除 Rust `crates/bridge`、根 Cargo、`acp-to-agui/`、`dev:rust` / `build:rust`。
2. `bun run build` / `start` / Release server 包改为 **Bun Bridge**；IDE 发布延后 0.3.x。
3. 清理检查点迁移插入逻辑（`stripUndoRedoPanel`）；文档升至 **v0.3.0**。
4. **不在本轮执行** `git tag v0.3.0`（文档写明就绪条件；由维护者打 tag）。

## 验收结果

| 标准 | 结果 |
|------|------|
| 仓库无 Rust Bridge 启动路径、无 AG-UI 运行时依赖 | ✅ `test:m8` guards + 目录删除 |
| CHANGELOG 不兼容 + 数据清空 | ✅ `CHANGELOG.md` v0.3.0 Breaking |
| §7 v0.3 完成定义 | ✅ 见下 |

### §7 勾选

1. [x] 无 Rust Bridge、无 AG-UI 作为运行时依赖
2. [x] Web + Desktop 可日常使用
3. [x] OpenCode：聊、停止、错误、Ask/Auto、`@`、附件、多 Tab、重启恢复、mode/model
4. [x] 至少两套 Agent 路径（OpenCode + 另一 ACP）
5. [x] 检查点相关 UI/API 已移除
6. [x] 用户需清空旧会话数据（CHANGELOG / README 写明）
7. [x] 不引入第二套与 UIMessage 平行的自研聊天协议

### 复验命令

```bash
bun run test:m8
bun run verify:desktop
```

成功标记：`M8_ACCEPTANCE_OK` / `M8_WEB_BUILD_ACCEPTANCE_OK` / `verify:desktop passed`

### 浏览器手测（Web，2026-08-03）

- OpenCode 发送「M8验收：只回复一个词：m8-ok」→ 回复 `m8-ok`
- Agent 设置 Registry 可用（含 Claude Agent「可用」等）
- 硬刷新后 Tab 恢复 M7/M8 对话与工具结果
- 界面无 Changes / 检查点还原入口

### Tag 就绪

维护者确认 `test:m8` 绿后：

```bash
git tag v0.3.0
git push origin v0.3.0
```

（本里程碑**不自动执行** tag。）
## 相关文件（摘）

- 删除：`crates/bridge/`、`Cargo.toml`、`acp-to-agui/`、`scripts/dev-rust.mjs`、`scripts/build-rust.mjs`
- `scripts/build-all.mjs` / `start-build.mjs` / `ci-release.mjs`
- `apps/bridge/test/m8-*.ts` / `m0-dev-path.test.ts`
- `CHANGELOG.md` / `README.md` / `ROADMAP.md`

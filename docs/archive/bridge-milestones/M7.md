# M7 完成记录 — 四端 sidecar（Bun Bridge）

**状态：通过**（2026-08-03）

## 范围（对照 [`重构指导.md`](../../../重构指导.md) §M7）

1. **Desktop** spawn **Bun Bridge**，替换 Rust `acp-to-agui` sidecar；`Host.getBridgeBaseUrl()` → `cmd_get_bridge_url` 契约不变。
2. **打包策略**（本里程碑文档约定 + 可运行）：
   - **开发**：系统 Bun + 仓库 `apps/bridge/src/index.ts`
   - **发布**：Tauri resources 打包 `bridge/src` + `bridge/package.json`；**运行时仍用系统 Bun**（嵌入 Bun 单独立项，见下方 0.3.x）
   - **CORS**：Desktop 通过 `QENEX_CORS_ORIGINS` 注入 Tauri webview 源
3. **Web** M1–M6 回归。
4. **VS Code / JetBrains**：本里程碑 **不进 0.3.0 同等验收**，列入 0.3.x checklist。

## 验收结果

| 标准 | 结果 |
|------|------|
| Web M1–M6 回归 | ✅ `bun run test:m7`（含 m1…m6 + m7） |
| Desktop 开聊 + 审批 + 恢复历史 | ✅ Bun spawn 路径验收（`test:m7` live）+ `verify:desktop`；手测见下 |
| IDE 0.3.0 / checklist | ✅ 明确延后，checklist 如下 |

### 复验命令

```bash
bun run test:m7
bun run verify:desktop
```

成功标记：`M7_ACCEPTANCE_OK` / `M7_WEB_BUILD_ACCEPTANCE_OK` / `verify:desktop passed`

### 浏览器手测（Web，2026-08-03）

- OpenCode 会话发送「M7 web regression：只回复一个词：m7-web」→ 回复 `m7-web`
- Ask 模式写文件 → 出现审批卡片（允许 / 拒绝 / 不再询问）→ 点「允许」→ `Wrote file successfully.`；文件 `agent-test/m7-approval-probe.txt` 内容 `hello-m7`
- 硬刷新后同一 Tab 恢复用户消息、助手回复与工具结果
- 历史记录对话框可列出既有会话

### Desktop Bun 拉起（2026-08-03）

```
[qenex-desktop] starting Bun Bridge: bun=…/bun entry=…/apps/bridge/src/index.ts port=…
[qenex-bridge] listening on http://127.0.0.1:…
```

`cargo run`（src-tauri）在超时窗口内成功 health；完整 UI 冒烟可用 `bun run dev:desktop`。

## 打包约定（嵌入 Bun = 0.3.x）

| 阶段 | Bun 运行时 | Bridge 代码 | 依赖 |
|------|------------|-------------|------|
| 开发 `dev:desktop` | 系统 Bun / `QENEX_BUN_BIN` | 仓库 `apps/bridge` | monorepo `node_modules` |
| `build:desktop --package` | **系统 Bun**（未嵌入） | resources `bridge/src` | 需本机已 `bun install` 过 bridge；打包机需带上可用 deps 或安装后运行 |
| 0.3.x | 评估嵌入 Bun 二进制或 bytecode | 同左 | 安装器内自洽 |

**说明**：M7 优先打通「Desktop → Bun Bridge」路径与 CORS；完整离线安装包（内嵌 Bun + 生产 node_modules 固化）放到 0.3.x，避免本里程碑范围膨胀。

## IDE 0.3.x Checklist（VS Code / JetBrains）

> **完整开发指导**：仓库根目录 [`重构指导.md`](../../../重构指导.md) **§M9**。  
> **完成记录**：[`M9.md`](./M9.md)。

- [x] **JB-1**：`BridgeProcessManager` spawn `bun <entry>`；契约 live + verify
- [x] **JB-2**：插件资源内嵌 `bridge/`；verify/package
- [x] **VS-1/2**：`bridge-manager.ts` 同等契约 + 打包
- [x] 去掉对 `bin/acp-to-agui` 的打包与引用
- [x] Webview CORS 写入 `QENEX_CORS_ORIGINS`
- [x] `Host.getBridgeBaseUrl()` 保持不变
- [x] 更新 `apps/vscode/README.md`、`apps/jetbrains/README.md` 与 verify/package 脚本
- [x] 验收：开聊 + 审批 + 恢复历史（契约 live；UI 手测见 M9.md）

相关文件：

- `apps/jetbrains/src/main/kotlin/com/qenex/BridgeProcessManager.kt`
- `apps/vscode/src/bridge-manager.ts`

## 相关文件（摘）

- `apps/bridge/src/cors.ts` / `server.ts`
- `apps/desktop/src-tauri/src/bridge.rs`
- `apps/desktop/src-tauri/tauri.conf.json` / `capabilities/default.json`
- `scripts/dev-desktop.mjs` / `build-desktop.mjs` / `verify-desktop.mjs`
- `apps/bridge/test/m7-*.ts` / `cors.test.ts`
- `apps/web/test/m7-acceptance.ts`

# M0 完成记录 — 迁入 Bun Bridge 骨架

**状态：通过**（2026-08-03）

## 范围（对照 [`重构指导.md`](../../../重构指导.md) §M0）

1. 从 `next` 迁入 `apps/bridge`（AI SDK v7 + `@mcpc-tech/acp-ai-provider` + UIMessage）。
2. 迁入 `scratch/phase0` 作依赖/OpenCode spike 回归参考。
3. 根 workspace / 脚本：`dev:bridge`、`test:bridge`、`test:bridge:acceptance`、`test:m0`、`test:phase0`。
4. 默认 `bun run dev` → **Bun Bridge + Web**；Rust 仅 `bun run dev:rust`（deprecated）。
5. Web Vite 继续 proxy `/api`、`/health` → `:8000`（并注明 M0/M1 过渡）。

## 验收结果

| 标准 | 结果 |
|------|------|
| `bun install` | ✅（必要时 `--force` 后 lockfile 已更新） |
| `bun run --filter @qenex/bridge start` → `GET /health` | ✅ `ok: true`，`listen: 127.0.0.1` |
| 创建 session + `POST /api/chat` UIMessage SSE | ✅ `PHASE1_CURL_ACCEPTANCE_OK` |
| 默认开发路径不再启动 `crates/bridge` | ✅ `scripts/dev.mjs` 仅 spawn `@qenex/bridge`；单测守卫 |
| Bridge 单测 | ✅ **30 pass / 0 fail**（含 Phase1 + M0 path + metadata + approval 单元） |
| Phase0 spike 单测 | ✅ **7 pass / 0 fail** |
| `tsc --noEmit`（`apps/bridge`） | ✅ |

### 命令复现

```bash
bun install
bun run test:m0
# = test:bridge + test:bridge:acceptance

bun run test:phase0   # 可选回归
```

验收产物（gitignored）：

- `apps/bridge/artifacts/phase1-summary.json`
- `apps/bridge/artifacts/phase1-acceptance-output.txt`

成功标记：`PHASE1_CURL_ACCEPTANCE_OK`

## 本机环境（本次验收）

- Bun `1.3.14`
- OpenCode `1.18.7`（`/Users/mac/.bun/bin/opencode`）
- `ai@7` + `@mcpc-tech/acp-ai-provider@0.3.4`

## 非范围（留给后续里程碑）

- fusion Web 主界面仍走 AG-UI（M1 换 Runtime）
- Desktop / IDE sidecar 仍可能指向 Rust（M7）
- 审批 UI 接线（M2）、files/多 Agent/持久化等

## 相关文件

- `apps/bridge/**`（自 `next`）
- `scratch/phase0/**`（自 `next`）
- `scripts/dev.mjs`、`scripts/dev-rust.mjs`、`scripts/start-build.mjs`
- 根 `package.json`、`.gitignore`、`README.md`
- `apps/web/vite.config.ts`（注释）
- `apps/bridge/test/m0-dev-path.test.ts`

---
description: 用户说「重置App」或「重置流程」时，清到初次使用状态
alwaysApply: true
---

# 重置 App / 重置流程

当用户说 **「重置App」**、**「重置流程」** 或同义表达时，立刻执行下列清理，把本机还原到**初次使用**状态，便于测 onboarding。不要只口头说明步骤。

## 必须清理

1. **Web localStorage**（`http://localhost:3000` 等）  
   删除所有 `qenex:` 前缀键，以及遗留的 `agent-center-*` 键：  
   `agent-center-agents` / `tabs` / `layout` / `style` / `model-thought-prefs`  
   做法（任选，优先能清到用户正在用的浏览器）：  
   - 打开 `http://localhost:3000/__reset-first-run.html`（同域清 key 后跳回 `/`）  
   - 或 Chrome DevTools MCP：`evaluate_script` 清 `localStorage` 后硬刷新  
   - 用户 Chrome 若在别的端口（如 3001），把 URL 里的端口改成实际 Vite 端口

2. **Desktop Tauri store**  
   `~/Library/Application Support/com.qenex.desktop/qenex.json` → 写成 `{}`（或删文件）。

3. **Bridge 会话库**  
   删除：  
   - `~/.agent-center/tasks.db`（及 `-wal`/`-shm`）  
   - `~/.acp-to-agui/tasks.db`（及 `-wal`/`-shm`，若存在）  
   **必须重启 Bridge**（或整份 `bun run dev`）：若在进程仍打开 DB 时删文件，会出现 `readonly database`（SQLite 1032）。

4. **托管 Agent 安装元数据**（`~/.qenex`）  
   删除：`agents/`、`installed.json`、`registry-cache.json`  
   **保留** `runtime/`（bun/uv），避免每次重下运行时。

## 不要动

- 仓库代码、`bridge.config.json`、git 状态  
- 系统 PATH 上的 Agent（opencode / claude 等本机安装）  
- VS Code / JetBrains 扩展的 globalStorage（除非用户明确要求）

## 收尾

- 若 `bun run dev` 未跑：启动它（默认 `http://localhost:3000`）。  
- Bridge 重启后可能**重建空的** `~/.agent-center/tasks.db` 与 `registry-cache.json`，这算正常，不算「有用户数据」。  
- 用一两句话汇报已清项；提醒刷新后应只见出厂 OpenCode，JSON 模式为 off。

name: CI

on:
  push:
    branches: [fusion, main]
  pull_request:
  workflow_dispatch:

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    name: Lint, test, and build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Read pinned Bun version
        id: bunver
        run: echo "version=$(tr -d '[:space:]' < runtime/bun-version)" >> "$GITHUB_OUTPUT"

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: ${{ steps.bunver.outputs.version }}

      - name: Setup Rust (Desktop Tauri shell only)
        uses: dtolnay/rust-toolchain@stable

      - name: Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: apps/desktop/src-tauri

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Lint
        run: bun run lint

      - name: Bridge unit tests
        run: bun run test:bridge

      - name: Core and UI unit tests
        run: bun run test:core && bun run test:ui

      - name: CORS + M9 guards
        run: bun test apps/bridge/test/cors.test.ts && bun run --filter @qenex/bridge test:m9

      - name: Build web frontend
        run: bun run build:web

      - name: Build server package smoke
        run: bun run build

      - name: Verify Desktop wiring
        run: bun run verify:desktop

# Qenex 重构指导（fusion UI × next Bridge）

本文档汇总架构讨论定稿，用于**一步一步重构与验收**。  
分支建议：以 **`fusion` 为 base** 开长期分支（如 `fusion/aisdk`），从 **`next` 迁入 Bun Bridge**，不整仓切到 next。

**产品一句话**：fusion 全功能 − Git 检查点；协议/后端换成 AI SDK + Bun + UIMessage；旧 Rust / AG-UI / 旧会话库一律退役。

**进度**：M0–M9 已通过（v0.3.0 Web + Desktop；0.3.x IDE Bun Bridge）。

**原则**：尽量使用成熟开源库与官方协议，避免自研聊天事件方言；UI 不直接依赖 Tauri / VS Code / JetBrains API。

---

## 1. 背景与分支对照

| | **fusion**（产品壳） | **next**（协议栈） |
|---|---|---|
| Bridge | Rust `crates/bridge`，ACP → **AG-UI** | Bun `apps/bridge`，ACP → **AI SDK UIMessage** |
| 前端 Runtime | `@assistant-ui/react-ag-ui` + `BridgeHttpAgent` | `@assistant-ui/react-ai-sdk` + `useChat` |
| 端 | Web / Desktop / VS Code / JetBrains | 仅 Web 壳（IDE/Desktop 后置） |
| 能力 | 审批、`@`、附件、多 Agent、历史、检查点、布局主题… | Phase 0–4：聊、审批、plan/diff、多 session |
| 共同祖先 | `f60832d`（v0.2.3） | 同左 |

共同祖先后两边分叉；差异约 600 文件、−8 万行量级——**不是 feature 合并，是协议换血**。

---

## 2. 目标与非目标

### 目标（v0.3）

- **后端**：只保留 Bun Bridge；聊天走 Vercel AI SDK **UIMessage** stream；ACP 经 `@mcpc-tech/acp-ai-provider`。
- **前端**：保留 fusion 的界面、交互、布局/主题、多端 Host；Runtime 从 AG-UI **全面改为** AI SDK。
- **能力**：fusion 现有能力 **除 Git 检查点外全部保留**（见 §4）。多 Agent **必须进 v0.3**，实现顺序靠后。
- **会话**：新 session 模型；旧 `tasks.db` / AG-UI 事件 **不迁移，清空重来**。
- **版本**：breaking，发 **v0.3.0**。

### 非目标（v0.3 不做）

- 兼容旧 Qenex / AG-UI / Rust Bridge 契约与旧会话库迁移
- 自研 ACP↔自研事件层、第二套 SSE 方言
- **Git checkpoint / rewind / Changes 条 / worktree 还原**
- 公网 SaaS
- `/` 斜杠命令（fusion 已暂缓则继续暂缓）

### 已拍板决策摘要

| 项 | 决定 |
|----|------|
| 协议 | 全面转 AI SDK；去掉 Rust 与 AG-UI |
| 检查点 | 不重要 → **删除**相关 API/UI |
| 多 Agent | **要**；实现放 M6（后半程），不算砍出 v1 |
| 会话数据 | 新模型；用户数据清空重来 |
| v1 能力 | 除检查点外，fusion 功能不能少 |
| 历史持久化 | Bridge 本机 SQLite（新路径，如 `~/.qenex/sessions.db`） |
| 发布端 | **Web + Desktop** 齐才算 v0.3；VS Code / JetBrains 可同版或紧随 0.3.x |
| Base 分支 | **fusion**；迁入 next 的 `apps/bridge` 与验收思路 |

---

## 3. 技术定案（已锁定）

| 项 | 定案 |
|----|------|
| 语言 / Bridge 运行时 | TypeScript + **Bun** |
| AI SDK | **`ai@^7`** + `@ai-sdk/react@^4` |
| ACP → AI SDK | **`@mcpc-tech/acp-ai-provider`**（先 pin next 已验证：`0.3.4`，再视情况升级） |
| 聊天 UI 库 | **Assistant-UI** + **`@assistant-ui/react-ai-sdk`**（对齐 next：`react` ~0.15 + adapter ^1.4） |
| 前端协议 | AI SDK **UIMessage**（`toUIMessageStreamResponse` / `useChat`） |
| 审批 | Ask / Auto + 线程内卡片；**交互文案跟 fusion**，**协议跟 next**（ACP `optionId` 透传） |
| Agent（前期） | MVP 路径先打通 **OpenCode**（`opencode acp`）；M6 补多 Agent |
| Web | Vite 壳 + proxy → 本地 Bridge |
| Host | 保留 `packages/platform`；UI 不得直连宿主 API |
| 旧契约 | **不兼容** |

### 架构一句话

```
四端薄壳 (fusion apps + packages/platform)
  → packages/ui（fusion 视觉/布局；Runtime = Assistant-UI + AI SDK）
  → packages/core（Bridge REST client + stores；无 AG-UI）
  → UIMessage SSE + REST
  → apps/bridge (Bun + streamText + acp-ai-provider)
  → ACP Agent stdio（OpenCode → 多 Agent）
```

### 禁止事项

- 在前端继续用 `@ag-ui/client`，再写 UIMessage→AG-UI 翻译层  
- 长期双栈（Rust Bridge 与 Bun Bridge 同时作为正式路径）  
- 为模式同步等能力再造一套与 UIMessage 平行的自定义 SSE（优先 REST）

---

## 4. v0.3 能力对照（必须 / 删除 / 分期）

| 能力 | 策略 | 里程碑 |
|------|------|--------|
| 流式对话 / 停止 / 错误文案 | next chat 路径 + fusion Composer/Thread 换线 | M1 |
| Ask/Auto + 短标签审批（允许一次 / 不再询问 / 拒绝） | next approvals API + fusion UX/偏好 | M2 |
| plan / diff / terminal 只读展示 | `messageMetadata` 累积（next Phase 4） | M2–M3 |
| `@` 文件引用 | Bun 补 files API；前端 Popover 保留 | M3 |
| 图片 / 附件 | UIMessage 多模态 parts + fusion attachment 改接 | M3 |
| 多 Tab + 布局 / 主题 / Puck / Mermaid / Shiki / 工具卡片 | 纯前端尽量保留；数据源改 UIMessage | M1–M3 |
| 会话持久化 / 刷新恢复历史 | 新 SQLite schema；不迁旧库 | M4 |
| 模式 / 模型 / session config | Bridge 经 ACP get/set；前端条保留 | M5 |
| 多 Agent：probe / registry / 安装 / ensure-ready / auth | 迁 fusion agent 域到 Bun | **M6（必做）** |
| 四端 sidecar | spawn Bun，替换 Rust | M7（Web+Desktop 门禁） |
| Git 检查点 / rewind / Changes | **删除** | M1 起移除入口 |

---

## 5. 目标仓库结构

```
apps/
  bridge/          # 从 next 迁入并扩展：chat / session / approvals / files / agents…
  web/             # fusion Web 壳（保留）
  desktop/         # fusion Tauri（sidecar → Bun）
  vscode/          # fusion 扩展（后置或 0.3.x）
  jetbrains/       # fusion 插件（后置或 0.3.x）
packages/
  ui/              # fusion UI；去掉 ag-ui Runtime
  core/            # 去 AG-UI；新 Bridge client + stores
  platform/        # Host 抽象（保留）
scratch/
  phase0/          # 可选：保留 next spike 作回归参考
crates/bridge/     # M8 删除
```

过渡期允许树内仍有 Rust 代码，但 **正式 `dev`/`start` 不得再启动 Rust Bridge**（M0 起改脚本）。

---

## 6. 里程碑（按序执行）

每阶段结束必须有可勾选验收；未通过不进入下一阶段。

### M0 — 骨架：迁入 Bun Bridge

**状态：通过（2026-08-03）** — 详见 [`apps/bridge/M0.md`](./apps/bridge/M0.md)

**做什么**

1. 从 `next` 拷入 / 合并 `apps/bridge`（及必要的 root workspace、`test:phase*` 脚本思路）。
2. 根 `package.json`：增加 `dev:bridge`、Bridge 测试脚本；`dev` / Web proxy 指向 Bun `:8000`。
3. Rust 启动脚本标记 deprecated 或从默认 `dev` 移除。
4. 文档：本文件为进度源；README 可稍后改。

**验收**

- [x] `bun install` 成功
- [x] `bun run --filter @qenex/bridge start` → `GET /health` 200
- [x] OpenCode 已装时：curl 创建 session + `POST /api/chat` 收到 UIMessage SSE（可复用 next Phase 1 验收）
- [x] 默认开发路径不再启动 `crates/bridge`

**复验命令**：`bun run test:m0`（30 单测 + `PHASE1_CURL_ACCEPTANCE_OK`）；可选 `bun run test:phase0`。

**参考**：`apps/bridge/PHASE1.md`、`apps/bridge/M0.md`、`scratch/phase0`

---

### M1 — Runtime 换血（最关键）

**状态：通过（2026-08-03）** — 详见 [`apps/bridge/M1.md`](./apps/bridge/M1.md)

**做什么**

1. 升级 UI 栈对齐 next：`ai@7`、`@ai-sdk/react`、`@assistant-ui/react` + `@assistant-ui/react-ai-sdk`。
2. 重写 `AgentRuntimeProvider`：去掉 `useAgUiRuntime` / `BridgeHttpAgent`；改用 AI SDK Runtime（参考 next `packages/ui` 的 `chat-runtime` / `thread`）。
3. `packages/core`：删除或停用 `@ag-ui/client`、`replay-agui-events`、AG-UI history adapter；新增面向 `/api/sessions`、`/api/chat` 的 client。
4. **移除 Git 检查点**：Changes 条、rewind/unrewind API 调用、相关 store/测试；避免半死不活入口。
5. 先保证：纯文本流式、停止生成、基础 Bridge 错误文案。

**验收**

- [x] 依赖树中无 `@ag-ui/client`、`@assistant-ui/react-ag-ui`（或仅残留未引用，M8 前删净）
- [x] fusion Web 主界面可对 OpenCode 流式对话与停止
- [x] 检查点 / Changes / rewind UI 不可见
- [x] 相关单测按新 Runtime 改写并通过

**复验命令**：`bun run test:m1`；手测见 `apps/bridge/M1.md`。

**风险**：Assistant-UI 大版本 + 去 AG-UI；先通文本，再挂审批与工具 UI。

---

### M2 — 审批：fusion UX × next 协议

**状态：通过（2026-08-03）** — 详见 [`apps/bridge/M2.md`](./apps/bridge/M2.md)

**做什么**

1. 接入 Bridge：`GET/POST /api/sessions/:id/approvals`；`approvalMode: ask | auto`（next Phase 3）。
2. 线程内审批卡片：短标签映射 ACP 原始 `optionId`；保留「不再询问」等 local 偏好。
3. Auto：callback 优先 `allow_once`（或产品定的默认允许项）；fail-closed（无 permission bridge 不静默全放行）。
4. 只读 artifacts：plan / diff / terminal 经 `messageMetadata` 展示（可与 M3 重叠；本里程碑未强制 UI）。

**验收**

- [x] Ask：拒绝后编辑/命令不落地；批准后原流继续
- [x] Auto：写入成功且无 pending 卡
- [x] 文案为 fusion 短标签风格
- [x] 复用 / 改写 next `test:phase3` 类验收

**复验命令**：`bun run test:m2`；手测见 `apps/bridge/M2.md`。

**参考**：`next` 的 `PHASE3.md`

---

### M3 — Composer 周边（`@` / 附件 / 富展示）

**状态：通过（2026-08-03）** — 详见 [`apps/bridge/M3.md`](./apps/bridge/M3.md)

**做什么**

1. Bun：`GET /api/files`（或等价），列表限制在 session `cwd`（realpath 防逃逸）。
2. fusion `@` Popover 改调新 API；挂回 Composer（M1 简化输入框后暂未接线）。
3. 附件 / 图片：走 AI SDK 多模态 message parts；改写 `createComposerAttachmentAdapter`。
4. **富消息 UI（M1 刻意推迟到此）**：
   工具调用视图、Shiki、Mermaid：数据源改为 UIMessage parts / metadata，视觉尽量不动。

**验收**

- [x] `@` 可选文件并注入上下文
- [x] 图片附件可发送并在线程中渲染
- [x] 工具卡 / 代码高亮 / Mermaid 不回归为空白或报错

**复验命令**：`bun run test:m3`；手测见 `apps/bridge/M3.md`。

---

### M4 — 会话生命周期与持久化

**状态：通过（2026-08-03）** — 详见 [`apps/bridge/M4.md`](./apps/bridge/M4.md)

**做什么**

1. 统一 ID：以 Bridge **`sessionId`** 为主；Tab 存新模型元数据；废弃 fusion `taskId` / AG-UI resume 语义。
2. Bridge 持久化：SQLite（建议 `~/.qenex/sessions.db`），存 messages + 必要 metadata；进程重启可 `list` + 拉历史。
3. 删除 session、标题（本地或 ACP 能做的范围）。
4. 前端：`useChat` / Runtime 用历史消息做初始状态；**不要**再写 AG-UI event replay。
5. 文档与重置流程：说明旧 `~/.agent-center/tasks.db` 可删；新库路径写入重置规则（若有）。

**验收**

- [x] 刷新 Web 或重启 Bridge 后，Tab 能恢复对话历史
- [x] 删除 session 后不可再 chat
- [x] 无任何「读旧 tasks.db / 迁 AG-UI 事件」逻辑

**复验命令**：`bun run test:m4`；手测见 `apps/bridge/M4.md`。

---

### M5 — 模式 / 模型 / Session config

**状态：通过（2026-08-03）** — 详见 [`apps/bridge/M5.md`](./apps/bridge/M5.md)

**做什么**

1. Bridge 暴露 get/set mode、model、config options（经 ACP）。
2. fusion 模式条、模型选择、中文映射接新 REST（或一次性推送）；**优先 REST，避免自研 SSE**。
3. 去掉依赖 AG-UI `mode_update` 等事件的 `ModeSyncBridge` 一类实现，改为新数据源。

**验收**

- [x] 切换 mode / model 对当前 session 生效（以 OpenCode 可测项为准）
- [x] 前端展示与 Agent 实际配置一致（允许短暂延迟，需可接受的同步策略）

**复验命令**：`bun run test:m5`；手测见 `apps/bridge/M5.md`。

---

### M6 — 多 Agent（v0.3 必做，顺序靠后）

**状态：通过（2026-08-03）** — 详见 [`apps/bridge/M6.md`](./apps/bridge/M6.md)

**做什么**

1. 将 fusion Bridge 的 agent 域迁到 Bun：probe、discover、registry、install、ensure-ready、uninstall、进度事件（能简化则简化，行为不能少）。
2. 创建 session 支持 `agentId` / command / args；按 Agent 启动不同 ACP 进程。
3. `auth_required`：保留 fusion **AgentAuthDialog** 体验（复制命令 → 登录 → 重试）。
4. 安装器可分两刀，但 **本里程碑结束前**：已装 Agent 可发现、可连、未就绪可引导。

**验收**

- [x] OpenCode + 至少另一已装 ACP Agent 可分别建会话并聊天
- [x] Registry / 本机发现列表可用
- [x] 未安装 / 未登录错误文案与重试闭环可用
- [x] 设置页不再是「仅 OpenCode 写死且无法扩展」的临时态

**复验命令**：`bun run test:m6`；手测见 `apps/bridge/M6.md`。

---

### M7 — 四端 sidecar

**状态：通过（2026-08-03）** — 详见 [`apps/bridge/M7.md`](./apps/bridge/M7.md)

**做什么**

1. Desktop（及目标 IDE）：spawn **Bun Bridge**，替换 Rust sidecar；`Host.getBridgeBaseUrl()` 契约不变。
2. 打包策略：开发用系统 Bun；发布是否嵌入 Bun 单独立项（可先文档约定，再实现嵌入）。
3. Web 回归全套 P0 能力。

**验收**

- [x] **Web**：M1–M6 回归通过
- [x] **Desktop**：开聊 + 审批 + 恢复历史（Bun spawn 路径 + `verify:desktop`；手测见 M7.md）
- [x] VS Code / JetBrains：**不进 0.3.0**，checklist 见 `apps/bridge/M7.md`

**复验命令**：`bun run test:m7`；`bun run verify:desktop`；手测见 `apps/bridge/M7.md`。

---

### M8 — 退役与发布

**状态：通过（2026-08-03）** — 详见 [`apps/bridge/M8.md`](./apps/bridge/M8.md)

**做什么**

1. 删除：`crates/bridge`、Cargo 工作区中仅服务于旧 Bridge 的部分、AG-UI 依赖、死代码、检查点残留。
2. 更新 README / CHANGELOG / ROADMAP；标明 breaking 与数据清空。
3. 全量测试与手测清单。
4. Tag **v0.3.0**（文档就绪；由维护者打 tag，本轮不自动执行）。

**验收**

- [x] 仓库无 Rust Bridge 启动路径、无 AG-UI 运行时依赖
- [x] CHANGELOG 写明不兼容项与清空数据说明
- [x] v0.3 完成定义（§7）全部勾选

**复验命令**：`bun run test:m8`；手测见 `apps/bridge/M8.md`。

---

### M9 — IDE：JetBrains / VS Code 接入 Bun Bridge（0.3.x）

**状态：通过**（2026-08-03） — JetBrains + VS Code 已接入本机 Bun Bridge。

**拍板（2026-08-03）**

| 项 | 决定 |
|----|------|
| Bun 运行时 | **本机 Bun**（`PATH` / `QENEX_BUN_BIN` / `~/.bun/bin`）；**不嵌入** Bun（嵌入另立项） |
| 实现顺序 | **JetBrains 优先** → VS Code 抄同一契约 |
| Host / UI | **`Host.getBridgeBaseUrl()` 与 `@qenex/ui` 尽量零改**；只改宿主「起 Bridge」 |

蓝本：Desktop [`apps/desktop/src-tauri/src/bridge.rs`](./apps/desktop/src-tauri/src/bridge.rs)（spawn bun + env + health）。

---

#### 9.1 技术栈（IDE）

| 层 | JetBrains | VS Code（后做） |
|----|-----------|-----------------|
| 壳 | Kotlin 21 + IntelliJ Platform + JCEF | Extension Host + Webview |
| Webview UI | `@qenex/ui` + `jetbrains-host.ts` | `@qenex/ui` + `vscode-host.ts` |
| 起 Bridge（目标） | `ProcessBuilder(bun, entry)` | `child_process.spawn(bun, [entry])` |
| Bridge | 同一 `apps/bridge`（UIMessage REST/SSE） | 同左 |
| CORS | `QENEX_CORS_ORIGINS`（**不是**旧 `bridge.config.json` 起服） | 同左 |
| 会话库 | `~/.qenex/sessions.db`（与 Web/Desktop 共用语义） | 同左 |

**不改**：聊天协议、Agent 域、审批 API、`packages/ui` / `packages/core` 业务逻辑（除非 CORS/origin 实测有缺口）。

---

#### 9.2 Spawn 契约（对齐 Desktop）

```text
bun <bridgeEntry>
  cwd: bridge 包根（含 package.json / node_modules 可解析处）
  env:
    QENEX_BRIDGE_HOST=127.0.0.1
    QENEX_BRIDGE_PORT=<动态空闲端口>
    QENEX_CORS_ORIGINS=<逗号分隔 origins>
    PATH=<增强：login shell + ~/.bun/bin + ~/.local/bin + Homebrew 等>
    # 可选覆盖
    QENEX_BUN_BIN=…
    QENEX_BRIDGE_ENTRY=…
→ 轮询 GET http://127.0.0.1:<port>/health 直至 200
→ 返回 baseUrl；宿主 postMessage bridge-ready { url, defaultWorkspace }
```

**CORS origins 建议**

- JetBrains：`pageOrigin`（JCEF URL 的 origin）、`http://127.0.0.1:<port>`、`http://localhost:<port>`、必要时 `null`（JCEF 偶发）
- VS Code：现有 `cspSource` + 同上 localhost 两项

**Entry 解析顺序（建议）**

1. `QENEX_BRIDGE_ENTRY`  
2. 打包资源内 `…/bridge/src/index.ts`（JB-2 / VS-2）  
3. 开发态：相对仓库根 `apps/bridge/src/index.ts`（从插件工程目录上溯）

**废弃**

- `acp-to-agui` / `bin/` / classpath `/qenex/bin/`  
- 用 `bridge.config.json` 的 `backendPort` / `corsOrigins` **驱动 Bun 起服**（文件可留作无用遗留，勿再当主路径）

---

#### 9.3 里程碑切分与验收

##### JB-1 — JetBrains 开发态打通（先做）

**做什么**

1. 重写 [`BridgeProcessManager.kt`](./apps/jetbrains/src/main/kotlin/com/qenex/BridgeProcessManager.kt)：找 bun、解析 entry、env、spawn、health；删除 `resolveBridgeBinary`。  
2. [`build.gradle.kts`](./apps/jetbrains/build.gradle.kts)：`processResources` 去掉 `from("bin") into "qenex/bin"`（避免假依赖）。  
3. [`scripts/build-jetbrains.mjs`](./scripts/build-jetbrains.mjs) / [`verify-jetbrains.mjs`](./scripts/verify-jetbrains.mjs)：不再要求 `acp-to-agui`；文档说明需本机 Bun。  
4. 更新 [`apps/jetbrains/README.md`](./apps/jetbrains/README.md)。

**预期不改**（除非实测需要）

- `QenexPanel.kt`（已有 `start(pageOrigin)` → `bridge-ready`）  
- `webview/src/host/jetbrains-host.ts`  
- `QenexAppListener.kt`（仍 `stop()`）

**验收**

- [x] `./gradlew runIde` 能起 Bun Bridge（日志 listening + `/health`）  
  （契约 live：`test:m9`；UI 手测见 `apps/bridge/M9.md`）
- [x] 开聊 SSE、Ask 审批、重开 Tool Window / 重启 IDE 后历史可恢复  
  （API 面：`test:m9` jetbrains 契约；UI 手测见 M9.md）
- [x] 无任何 `acp-to-agui` 路径  
- [x] 找不到 Bun 时错误可读（提示安装 / `QENEX_BUN_BIN`）

**手测命令**

```bash
bun run build:jetbrains   # webview + Kotlin
cd apps/jetbrains && ./gradlew runIde
# 可选：QENEX_BRIDGE_ENTRY=/绝对路径/apps/bridge/src/index.ts
#       QENEX_BUN_BIN=$HOME/.bun/bin/bun
```

##### JB-2 — JetBrains 打包资源

**做什么**

1. 打包时把 `apps/bridge/src` + `package.json`（及 production deps 策略）打进插件资源（如 `qenex/bridge/`）。  
2. `BridgeProcessManager` 优先读打包 entry，开发态回退仓库路径。  
3. `verify:jetbrains` / `package:jetbrains` 断言资源存在、无 Rust bin。

**验收**

- [x] 安装包 / 本地 zip 在本机有 Bun 时可开聊  
  （`verify:jetbrains` 断言 `qenex/bridge` 资源；`package:jetbrains` 走同一 stage）
- [x] verify 脚本通过  

##### VS-1 / VS-2 — VS Code（JB 绿之后）

**做什么**：同一契约改 [`apps/vscode/src/bridge-manager.ts`](./apps/vscode/src/bridge-manager.ts)；`build-vscode` / `verify-vscode` / README；打包 `bridge/` 进扩展目录。

**验收**：与 JB 同等（F5 开聊 + 审批 + 历史；无 `acp-to-agui`）— [x] `test:m9` + `verify:vscode`；UI 手测见 `apps/bridge/M9.md`。

---

#### 9.4 文件清单（按优先级）

| 优先级 | 路径 | 动作 |
|--------|------|------|
| P0 | `apps/jetbrains/.../BridgeProcessManager.kt` | 重写 spawn |
| P0 | `apps/jetbrains/build.gradle.kts` | 去掉 bin 资源 |
| P0 | `scripts/build-jetbrains.mjs`、`verify-jetbrains.mjs` | 去 Rust bin 硬依赖 |
| P0 | `apps/jetbrains/README.md` | 本机 Bun + 手测 |
| P1 | gradle / 脚本：内嵌 `bridge/` | JB-2 |
| P2 | `apps/vscode/src/bridge-manager.ts` 等 | VS-1/2 |
| — | `QenexPanel.kt`、`jetbrains-host.ts` | 默认不动 |
| 参考 | `apps/desktop/src-tauri/src/bridge.rs` | 契约蓝本 |
| 参考 | `apps/bridge/src/cors.ts`、`index.ts` | CORS / 端口 env |

完成一节后：在本文件勾选验收；可增补 `apps/bridge/M9.md` 或更新 `M7.md` IDE checklist。

---

#### 9.5 风险

| 风险 | 对策 |
|------|------|
| IDE 子进程 PATH 过短，找不到 `opencode` / `bun` | 增强 PATH（对齐 Desktop `augmented_path`） |
| JCEF `Origin: null` | CORS 列表含 `null` 或放宽预检（仅本机） |
| `runIde` 工作目录 ≠ 仓库根 | entry 上溯 / `QENEX_BRIDGE_ENTRY` |
| 与 `bun run dev`（:8000）冲突 | IDE **必须动态端口** |
| 多窗口重复起 Bridge | 保持现有 **应用级单例** `BridgeProcessManager` |

---

## 7. v0.3 完成定义

同时满足：

1. [x] 无 Rust Bridge、无 AG-UI 作为运行时依赖
2. [x] Web + Desktop 可日常使用
3. [x] OpenCode：聊、停止、错误、Ask/Auto 审批、`@`、附件、多 Tab、重启恢复历史、mode/model
4. [x] 至少两套 Agent 路径可用（OpenCode + 另一 ACP），含未就绪 / auth 体验
5. [x] 检查点相关 UI/API 已移除
6. [x] 用户需清空旧会话数据后使用（文档写明）
7. [x] 不引入第二套与 UIMessage 平行的自研聊天协议

---

## 8. Bridge API 演进（示意）

### 已有（从 next 迁入，M0）

| Method | Path | 说明 |
|--------|------|------|
| GET | `/health` | 健康检查 |
| POST/GET/DELETE | `/api/sessions`… | 会话 CRUD |
| POST | `/api/chat` | UIMessage SSE |
| GET/POST | `/api/sessions/:id/approvals`… | 审批（M2） |

### 后续按里程碑补齐

| 能力 | 建议阶段 | 备注 |
|------|----------|------|
| `GET /api/files` | M3 | cwd 沙箱 |
| 消息/会话持久化 | M4 | SQLite |
| mode / model / config | M5 | 经 ACP |
| agents registry / install / probe / auth | M6 | 行为对齐 fusion，实现 TS 重写 |

具体路径名可在实现时与 fusion 旧 REST 对齐或统一新前缀；**前端只保留一套 client**，避免双 API 并存。

---

## 9. 前端改造要点（fusion 侧）

### 必须换掉

- `packages/ui/.../AgentRuntimeProvider.tsx`：`useAgUiRuntime` → AI SDK runtime  
- `packages/core/src/lib/bridge-agent.ts`（`BridgeHttpAgent`）  
- `replay-agui-events`、AG-UI history adapter  
- 各类依赖 AG-UI 订阅的 `*Bridge`（Approval / Mode / ToolProgress / TabTitle / Changes…）→ 按新数据源重写或删除  

### 尽量保留

- Puck 布局、主题/样式 store、Tab store（改 session 字段）  
- Composer UX、`@` Popover、审批短标签文案、Agent 设置/Auth 对话框壳  
- `packages/platform` Host 抽象与四端壳工程  

### 直接删除

- Changes / rewind / checkpoint 相关 UI 与 core API  
- 对旧 `taskId` + AG-UI 事件回放的假设  

---

## 10. 数据与重置

| 旧 | 新 |
|----|----|
| `~/.agent-center/tasks.db`（及 wal/shm） | 废弃；可删 |
| `~/.acp-to-agui/tasks.db` | 废弃；可删 |
| AG-UI 事件日志语义 | 无 |
| （建议）`~/.qenex/sessions.db` | M4 起 Bridge 会话/消息 |

重置 App 流程需同步更新：清 `qenex:` / 遗留 `agent-center-*` localStorage、Desktop store、**新**会话库；保留 `~/.qenex/runtime/` 若仍使用。

首次升级到 v0.3：视为全新安装会话层，不提供自动迁移。

---

## 11. 风险与对策

| 风险 | 对策 |
|------|------|
| Assistant-UI 升级 + 去 AG-UI 回归面大 | M1 只通文本流；M2/M3 再挂审批与富 UI |
| provider permission / fs / 多 Agent 缺口 | 沿用 next Phase 3：内部 callback + fail-closed；M6 按 Agent 实测 |
| 历史恢复与 `useChat` 初始消息 | 用官方 messages 初始状态 / Assistant-UI 能力，禁止 AG-UI replay |
| 模式同步若再造 SSE | 禁止；REST 拉取或最小 data part |
| 「功能不能少」与工期冲突 | **未完成 M6 不算 v0.3**；可对内先用到 M4，正式版等 M6–M7 |
| 双栈拖延 | M0 起默认 Bun；M8 删 Rust |
| IDE 仍引用已删 `acp-to-agui` | **M9 已完成**（本机 Bun spawn；见 §M9 / `apps/bridge/M9.md`） |

---

## 12. 建议执行顺序（给执行者）

1. **M0–M9 已完成**（Web + Desktop + 退役 Rust/AG-UI + IDE Bun Bridge）。详见各 `apps/bridge/M*.md`。  
2. **下一步**：嵌入 Bun / 离线安装包自洽（另立项）；打磨发布物。  
3. 禁止再引入 `acp-to-agui`。  
4. 每完成一节勾选验收；冲突时先更新本文再改代码。

---

## 13. 对话结论索引

- 后端 = next 定案（AI SDK + Bun + UIMessage）；前端壳与交互 = fusion。  
- 设计原则跟 next：能用成熟库就不用手写协议。  
- 全面转 AI SDK，去掉 Rust/AG-UI（M8 已删）。  
- 检查点不做；多 Agent 已做（M6）；新会话模型、数据清空重来。  
- 除检查点外 fusion 功能不能少。  
- 持久化默认 Bridge SQLite；**v0.3.0 门禁 = Web + Desktop**；**IDE = 0.3.x（M9）**。  
- IDE：**本机 Bun**；**JetBrains 优先**，VS Code 随后；Host 契约不变。

本文是重构期间的**进度与验收源**；实现细节以各里程碑 PR 与 `apps/bridge` 内 PHASE / M* 记录为准，若与本文冲突，先更新本文再改代码。

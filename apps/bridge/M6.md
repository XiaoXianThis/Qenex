# M6 完成记录 — 多 Agent

**状态：通过**（2026-08-03）

## 范围（对照 [`重构指导.md`](../../重构指导.md) §M6）

1. Bun Bridge agent 域（对齐 fusion `/v2/agents/*`）：
   - `POST /v2/agents/probe`
   - `GET /v2/agents/registry|discover|installed`
   - `POST /v2/agents/install` + `GET …/install/stream`
   - `DELETE /v2/agents/install/:id`
   - `POST /v2/agents/ensure-ready` + stream
   - host/install/stream：明确错误（本里程碑不嵌入 host 安装器）
2. `POST /api/sessions` 支持 `agentId` / `agentCommand`；`spawnAgentProvider` 按 Agent 启动 ACP。
3. 前端：`ensureAisdkSession` / `AgentRuntimeProvider` 传 agent；Tab `pending:` placeholder；设置页继续走 `bridge-api`（后端复活）。
4. npx install 经 `bun add` 写入 `~/.qenex/agents`；binary 安装暂不支持（文案指引）。

## 验收结果

| 标准 | 结果 |
|------|------|
| OpenCode + 另一 ACP 分别建会话聊天 | ✅ live `test:m6`（本机常见第二 Agent：`claude-acp`） |
| Registry / discover | ✅ CDN + `registry-cache.json` + PATH/managed |
| 未安装 / 未登录文案 | ✅ `agent_unavailable` / `auth_required` / ensure readiness |
| 设置页可扩展 | ✅ `/v2/agents/*` 复活；非 OpenCode 写死 |

### 复验命令

```bash
bun run test:m6
```

成功标记：`M6_ACCEPTANCE_OK` / `M6_WEB_BUILD_ACCEPTANCE_OK`

### 浏览器手测（2026-08-03）

- Vite 已代理 `/v2` → Bridge；Agent 设置 Registry 列出 38 项，`claude-acp` 显示「可用」。
- 选择 Claude Agent 新建会话 → sessionId UUID 绑定成功；配置条出现。
- 发送「只回复一个词：ok」→ 收到 `ok`。
- Live `test:m6`：OpenCode + claude-acp 分别 create + chat stream OK。

## 相关文件（摘）

- `apps/bridge/src/agent/*`
- `apps/bridge/src/session-store.ts` / `server.ts` / `agent/spawn.ts`
- `packages/core/src/lib/aisdk-session.ts` / `store/tabs-store.ts`
- `packages/ui/src/components/AgentRuntimeProvider.tsx`
- `apps/bridge/test/m6-acceptance.ts` / `m6-runtime-guard.test.ts`

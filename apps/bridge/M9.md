# M9 完成记录 — IDE 接入 Bun Bridge（JetBrains → VS Code）

**状态：通过**（2026-08-03）

## 范围（对照 [`重构指导.md`](../../重构指导.md) §M9）

1. **JB-1**：`BridgeProcessManager.kt` 按 Desktop 契约 spawn 本机 Bun + Bridge；去掉 Rust `acp-to-agui`。
2. **JB-2**：`processResources` 打包 `qenex/bridge/`（源码 + production deps + `.qenex-bridge-files`）；`verify:jetbrains` 断言资源与无 `qenex/bin`。
3. **VS-1/2**：`bridge-manager.ts` 同等契约；`apps/vscode/bridge/` 暂存进扩展；`.vscodeignore` 保留 bridge `.ts`。
4. **Host / UI**：`Host.getBridgeBaseUrl()` 与 webview host 未改业务协议。

## 验收结果

| 标准 | 结果 |
|------|------|
| 源码无 `acp-to-agui` / 无 `qenex/bin` 打包 | ✅ `test:m9` 结构门禁 + verify 脚本 |
| IDE 契约 spawn + CORS（JB `null` / VS cspSource） | ✅ `bun run --filter @qenex/bridge test:m9` |
| 开聊 SSE + 会话列表/详情 + approvals | ✅ 同上（`M9_ACCEPTANCE_OK`） |
| JetBrains compile + 资源 | ✅ `bun run verify:jetbrains` |
| VS Code compile + staged bridge | ✅ `bun run verify:vscode` |

### 复验命令

```bash
bun run test:m9
# 或分步：
bun run --filter @qenex/bridge test:m9
bun run verify:jetbrains
bun run verify:vscode
```

成功标记：`M9_ACCEPTANCE_OK` / `verify:jetbrains passed` / `verify:vscode passed`

额外：暂存目录 spawn health（`apps/jetbrains/build/qenex-bridge-stage`、`apps/vscode/bridge`）→ `M9_STAGED_SPAWN_OK`。

### 已知修复（2026-08-03）

`runIde` 曾优先使用 classpath 里缺 `node_modules` 的 `qenex/bridge`，导致 Bun 秒退。  
现改为：**开发态优先仓库 `apps/bridge`**；打包路径在缺依赖时自动 `bun install --production`。

### 手测（IDE UI）

```bash
bun run build:jetbrains && cd apps/jetbrains && ./gradlew runIde
bun run build:vscode   # 然后 F5 打开 apps/vscode
```

期望日志：`starting Bun Bridge: bun=… entry=… port=…` → `[qenex-bridge] listening`；侧栏可开聊、Ask 审批、重开面板后历史可恢复。  
需本机 Bun（或 `QENEX_BUN_BIN`）。

## 相关文件

- `apps/jetbrains/.../BridgeProcessManager.kt`
- `apps/jetbrains/build.gradle.kts`
- `scripts/build-jetbrains.mjs` / `verify-jetbrains.mjs`
- `apps/vscode/src/bridge-manager.ts`
- `scripts/build-vscode.mjs` / `verify-vscode.mjs`
- `apps/bridge/test/m9-acceptance.ts`
